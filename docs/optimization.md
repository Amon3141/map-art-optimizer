# 最適化（将来）

**現状: 未実装。**

## 想定インプット（予定）

- 簡略化済みの単一ストローク（2D 点列、キャンバス正規化座標など）
- 探索エリア（地図上の bbox または中心＋半径）
- 距離の目安（整数 km、厳密ではなく ±2 km 程度を将来の目標とする）

## 想定アウトプット（予定）

- 地図上の候補ルート（道路に沿った座標列）、および適用した位置・回転・スケール

## メモ

アルゴリズム選定・グラフ化・評価関数は実装フェーズで `docs/decisions.md` と本ファイルに追記する。
